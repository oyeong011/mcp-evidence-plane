{
  "name": "mcp-evidence-plane",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "engines": { "node": ">=24" },
  "scripts": {
    "test": "node --test packages/policy-engine/test/*.test.ts",
    "typecheck": "tsc --noEmit"
  }
}
